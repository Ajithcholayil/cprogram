#include <stdio.h>
int main()
{
printf("Enter a integer value: ");
scanf("%d",&value);
if(number %2==0);
printf("%d the number is even", number);
else
printf("%d" the number is odd", number);
return 0;
}
